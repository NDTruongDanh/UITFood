/**
 * Drizzle schema barrel for the Ordering database. drizzle-kit reads the domain
 * schema files directly (see drizzle.config.ts); this barrel is what the Nest
 * `drizzle(pool, { schema })` client uses for typed queries. Domain tables are
 * added here in Step 2 as the bounded context is moved in.
 */
export {};
