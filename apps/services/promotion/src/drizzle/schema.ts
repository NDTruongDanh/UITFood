/**
 * Drizzle schema barrel for the Promotion database. drizzle-kit reads this file
 * (see drizzle.config.ts) to generate migrations, so it uses a relative import.
 */
export * from '../promotion/domain/promotion.schema';
